[package]
name = "obsbot-meetse-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
libc = "0.2"

[build-dependencies]
cc = "1.0"
bindgen = "0.72"
